import { ActionReducerMap } from "@ngrx/store";

export interface AppState {
  // no state for the app per-se yet
}

export const appInitialState: AppState = {

};


export const APP_REDUCERS: ActionReducerMap<AppState> = {

};
